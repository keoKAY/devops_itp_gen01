## NOTE 

Justfile to group all the related commands into one name
one name = recipe 

inside one justfile can be many different recipes 


```bash
sudo snap install just --classic 
which just 
just --list # check available recipe 
just recipe_name # to run recipe 
```